# Reference folders (not part of this project)
Claude-StartUp/
Claude-StartUp-BackUp/

# Local Claude Code configuration
.claude/
.mcp.json
CLAUDE.md

# Runtime state (generated at startup)
state.json

# Local runtime config (generated from template)
config/config.json

# Agents API (Managed Plane) local config — approval IDs must never be committed
config/agents-api.json

# Test artifacts
testResults.xml

# Logs (generated at runtime)
logs/

# Config backups (generated at runtime)
config/backups/

# Recent projects history (per-user)
.codex-startup/

# Python test artifacts (tests/python)
__pycache__/
*.pyc
*.pyo
.pytest_cache/
