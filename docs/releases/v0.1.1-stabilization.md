# v0.1.1 Stabilization Notes

## Objective

v0.1.1 is the first stabilization pass after the v0.1.0 initial release. The goal is to validate real Supervisor rollout behavior without broad write operations across all registered projects.

## Scope

- Apply Supervisor to one real project first.
- Inspect `.codex/supervisor.json`.
- Apply Supervisor to three additional startup-tool projects.
- Verify Codex launcher dry-run behavior.
- Verify recent-project history and SUCCESS log generation using a stubbed launcher command.
- Keep final release/public visibility decisions with the human owner.

## Applied Projects

| Step | Project | Path |
|---|---|---|
| Initial single-project rollout | `Codex-StartUpTools-New-Linux` | `/home/kensan/Projects/Codex-StartUpTools-New-Linux/.codex/supervisor.json` |
| Additional rollout | `Claude-StartUpTools-New-Linux` | `/home/kensan/Projects/Claude-StartUpTools-New-Linux/.codex/supervisor.json` |
| Additional rollout | `ClaudeCode-StartUpTools-New` | `/home/kensan/Projects/ClaudeCode-StartUpTools-New/.codex/supervisor.json` |
| Additional rollout | `Codex-StartUpTools` | `/home/kensan/Projects/Codex-StartUpTools/.codex/supervisor.json` |

## Supervisor Manifest Checks

Confirmed values:

- `managedBy`: `Codex-StartUpTools-New-Linux`
- `mode`: `cto-autonomous`
- `codexOnly`: `true`
- `sshEnabled`: `false`
- `humanDecisionRequired`: `final-choice`, `merge`, `release`

## Launcher Smoke Checks

Completed:

- `./start.sh` menu startup and exit with `0`
- `Start-Codex.ps1 -Project Codex-StartUpTools-New-Linux -DryRun -NonInteractive`
- Stubbed non-interactive launch with `pwsh -NoProfile -Command exit 0`
- Recent project history saved with `result=success`
- Bootstrap and launch SUCCESS logs generated

## Safety Rules

- Do not use `all` for normal Supervisor rollout.
- Apply by numbered selection.
- Expand to business projects only after human review.
- Preserve human final decision for release, public visibility, merge, and broad deployment.
